# Canonical URL for Open Graph, Twitter cards, sitemap, and robots (no trailing slash).
# Required in production so link previews are not tied to localhost or a placeholder domain.
# On Cloudflare Pages you can set this to your custom domain, e.g. https://www.yoursite.com
NEXT_PUBLIC_SITE_URL=

# Password for protected pages/routes
PAGE_ACCESS_PASSWORD=password
